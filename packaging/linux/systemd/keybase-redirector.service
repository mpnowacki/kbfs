[Unit]
Description=Keybase Root Redirector for KBFS
Wants=keybase.service

[Service]
Type=simple

# Abort if root redirector is disabled
ExecStartPre=/usr/bin/run_keybase -r

# Do we need any special thing for nohup here?
ExecStart=/usr/bin/keybase-redirector /keybase
Restart=on-failure
